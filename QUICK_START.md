# 🚀 Швидкий старт проєкту

## Покрокові інструкції для запуску

### Крок 1: Встановити залежності React

Відкрийте термінал (PowerShell або Command Prompt) і виконайте:

```powershell
cd Testove\ClientApp
npm install
```

**Очікуваний результат:** Встановляться всі залежності (React, Vite тощо)

---

### Крок 2: Зібрати React проєкт

В тому ж терміналі виконайте:

```powershell
npm run build
```

**Очікуваний результат:** 
- Створиться папка `wwwroot/js/dist/`
- З'явиться файл `url-shortener.js`

**Якщо помилка:** Переконайтеся, що ви в папці `ClientApp`

---

### Крок 3: Запустити ASP.NET проєкт

Відкрийте **новий термінал** і виконайте:

```powershell
cd Testove
dotnet run
```

**Очікуваний результат:**
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: https://localhost:5001
      Now listening on: http://localhost:5000
```

---

### Крок 4: Відкрити браузер

Перейдіть на: **https://localhost:5001** або **http://localhost:5000**

---

## ✅ Перевірка, що все працює

1. ✅ Відкривається головна сторінка
2. ✅ Відображається таблиця URL (навіть якщо порожня)
3. ✅ В консолі браузера (F12) немає помилок
4. ✅ Можна увійти: `admin` / `admin`

---

## 🔧 Якщо щось не працює

### Помилка: "npm: command not found"
**Рішення:** Встановіть Node.js з https://nodejs.org/ (версія 18+)

### Помилка: "dotnet: command not found"  
**Рішення:** Встановіть .NET 8.0 SDK

### Помилка: "Cannot find module"
**Рішення:** 
```powershell
cd Testove\ClientApp
npm install
npm run build
```

### React не відображається
**Рішення:** Переконайтеся, що виконали `npm run build` і файл `wwwroot/js/dist/url-shortener.js` існує

### Порт зайнятий
**Рішення:** Змініть порт в `Properties/launchSettings.json`

---

## 📝 Для розробки (опціонально)

Якщо хочете hot reload під час розробки:

**Термінал 1:**
```powershell
cd Testove\ClientApp
npm run dev
```

**Термінал 2:**
```powershell
cd Testove
dotnet run
```

**УВАГА:** Для цього потрібно також змінити `Index.cshtml` на dev режим (зараз використовується production bundle)

---

## 🎯 Мінімальні команди для запуску

```powershell
# 1. Встановити залежності та зібрати React
cd Testove\ClientApp
npm install
npm run build

# 2. Запустити ASP.NET
cd ..
dotnet run
```

Готово! 🎉

